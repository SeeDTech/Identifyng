import { StyleSheet } from 'react-native'
import { BaseColor } from '../../styles/theme/color';

  

const navigations = StyleSheet.create({
    drawerColor:{
        color:BaseColor.dark
    }
})
export default navigations